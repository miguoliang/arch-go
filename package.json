{
  "scripts": {
    "generate:keycloak": "openapi-generator-cli generate --skip-validate-spec -i keycloak.yaml -g go -o keycloak/ --package-name=keycloakadminclient --git-repo-id=keycloakadminclient --git-user-id=miguoliang"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.13.2"
  }
}
